# V0.7 status

As of 2026-08-16:

```text
LOCAL: PASS — new semantic tests plus CPU cache/train/evaluate integration
GPU: PENDING — RTX 5080 multi-seed command not yet returned
SCIENTIFIC: PENDING — residual scale and history evidence require GPU artifacts
```

Base checkpoint identity, SHA-256, residual RMS/std/ACF/burden, full H sweep, parameter matching, closed-loop curves, three independent conclusions, and V0.8 route will be populated automatically in the per-ID GPU report. No V0.6 checkpoint is stored in Git.
