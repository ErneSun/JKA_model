# V0.7 compact results

Each successful ID receives `completion.json`, `summary.json`, `report.md`, primary `evaluation/residual_structure_assessment.json`, secondary `memory_classification.json`, `history_sweep.csv`, seven diagnostic plots, and both route reports. `completion.json` proves all 144 records, provenance, and reports completed. An incomplete session receives a stage-aware `failure.json`. Raw caches, logs, and checkpoints remain under ignored `runs/v0_7/<id>/`.
