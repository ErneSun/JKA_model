# V0.7 compact results

Each resolved validation ID receives `summary.json`, `report.md`, `evaluation/history_sweep.csv`, `evaluation/memory_classification.json`, diagnostic plots, and both decision reports. These small review artifacts are intended for Git. Raw caches, logs, and checkpoints remain under ignored `runs/v0_7/<id>/` on the GPU server.
