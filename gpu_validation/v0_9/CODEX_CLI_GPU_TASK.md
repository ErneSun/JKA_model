# Remote GPU task

1. Checkout the exact clean commit.
2. Activate the project `.venv`.
3. Confirm the strict-ready V0.8 raw run still exists under `runs/v0_8/`.
4. Run the single command from `README.md`.
5. Return `gpu_validation/v0_9/results/<resolved-id>/`; retain large raw checkpoints on the server.

Do not manually select rank from test results and do not add residual correction after a negative run.
