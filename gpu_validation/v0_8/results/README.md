# V0.8 compact results

Each formal run creates one `<resolved-id>/` directory containing `completion.json`, `report.md`,
`evaluation/`, `plots/` and `reports/`. These compact artifacts are reviewable and suitable for git;
large checkpoints and offline trajectories remain in `runs/v0_8/<resolved-id>/`.

